import React from 'react';
// components
import Layout from '../Layout';
import PairForm from './PairForm';

const Pair: React.FC = () => (
  <Layout>
    <PairForm />
  </Layout>
);

export default Pair;
